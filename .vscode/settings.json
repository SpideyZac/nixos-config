{
    "nixEnvSelector.nixFile": "${workspaceFolder}/flake.nix",
    "nix.enableLanguageServer": true,
    "nix.serverSettings": {
        "nil": {
            "formatting": {
                "command": [
                    "nixfmt"
                ]
            }
        }
    }
}